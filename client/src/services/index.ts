import { createClient, deleteClient, getClients, updateClient } from './clientService'
import {
   createProduct,
   deleteProduct,
   getProducts,
   updateProduct,
} from './productService'

export {
   createClient,
   createProduct,
   deleteClient,
   deleteProduct,
   getClients,
   getProducts,
   updateClient,
   updateProduct,
}
