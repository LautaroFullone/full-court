import { createClient, deleteClient, getClients, updateClient } from './clientService'
import {
   createProduct,
   deleteProduct,
   getProducts,
   updateProduct,
} from './productService'
import {
   createReservation,
   deleteReservation,
   getReservationsByDate,
   updateReservation,
} from './reservationService'

export {
   createClient,
   createProduct,
   deleteClient,
   deleteProduct,
   getClients,
   getProducts,
   updateClient,
   updateProduct,
   createReservation,
   deleteReservation,
   getReservationsByDate,
   updateReservation,
}
